# CHANGELOG

版本号只有一个:`master/Cargo.toml` 的 `version`。tag、它、以及本文件里的
`## v<x>` 标题三者必须一致 —— `release.yml` 会在打 tag 时校验,不一致直接 fail。
agent 是同一个版本号,通过 `-ldflags "-X main.Version=…"` 注入(§11.1)。

## v0.4.2

两条都是**用起来才发现的**,不是读代码能看出来的那类。

### 改

- **服务管理页多一列「出站」。** 出站策略原来只写在底部「操作」面板的摘要行里,
  而那一行**会被操作回执顶掉** —— 于是「按完 `[o]` 想确认改对没有」恰恰就是
  看不到它的那一刻;选中态一丢就更没处看了。

  现在表格里有自己一列:第一行是当前值(短名,「自动」/「优先v4」/「仅 v6」…),
  第二行写「`[o]` 改」。第二行不是废话 —— 这是个只读表格,不写的话没人知道
  这一列能改、更不知道按哪个键。非「自动」用高亮色,一眼能扫出哪几台被特意设过。

  窄屏上它比主机列**先保**:CPU / 内存是锦上添花,而出站策略是一个
  「改了就得确认」的设置项。120 列有出站没主机,140 列两个都有,
  70 列两个都让掉 —— 这个次序有测试盯着。

- **回执里不再印 `config_revision`。** 原来是
  `出站策略改成「自动」(配置版本 14,由 daemon 下发)`—— 那个 14 是内部计数器,
  每改一次加一,摆给人看只是一串越来越大的噪音。要紧的信息是**什么时候生效**,
  那半句留着。

  另外三处同样的毛病一起改了(新增 / 编辑 / 删除节点),它们原来都在回执里
  印 `config_revision → N`。

### 内部

- 有一条测试断言「回执里含 `config_revision`」—— 那不是在守什么保证,
  只是把当时的措辞抄了一遍,措辞一改它就红。改成断言「说清了什么时候生效」;
  这条测试真正值钱的部分(编辑节点**不能动密钥材料**,§9.1)原样保留。

- README 的 TUI 按键表补齐到当前状态(`[o]` / `[u]` / `[U]` / `[T]` / `[r]`
  和仪表盘的 `←/→` 换栏 + `Enter` 下钻),通用键提到表格上方单独说一次 ——
  它们原来在每一行里重复,把当页真正能做的事挤到了行尾。

  同时改正两处过期的说法:「仍未实测的只有 agent 自升级」不再成立(v0.4.0 起
  各段都有测试),但也没夸大 —— **真机端到端跑一次完整升级仍然没做过**,
  写清楚了。测试数目 394 / 46 改成 453 / 54,两个数都是**跑出来数的**。

## v0.4.1

### 新增

- **服务管理页 `[o]`:每台 agent 的出站地址族策略。** 五个取值 ——
  自动(跟随系统解析)/ 优先 IPv4 / 优先 IPv6 / 仅 IPv4 / 仅 IPv6,
  按一下切下一个,当前值显示在「操作」面板的摘要行里。

  按 agent 存而不是全局:这是「这台落地机怎么解析域名」的问题,天然一台一份 ——
  一台只有 IPv4 出口的机器要 `ipv4_only`,另一台双栈的可以 `prefer_ipv6`。

  **那个字段是静默失效的,不是报错。** sing-box 的 dial 字段 `domain_strategy`
  1.12.0 弃用、1.14.0 移除,而 agent 内嵌的正是 1.14.0-beta.3。
  照直觉会以为「写了会让 `box.New()` 失败」,**拿真 sing-box 试过之后发现不是**:
  带这个字段的 outbound 照常装配成功,`Check` 返回 nil,字段被悄悄丢掉。

  这比启动失败难查得多。写错的表现是:界面显示策略已改、配置确实下发了、
  agent 回执正常,而机器上的解析行为一如从前 —— 全链路没有任何一处会报错。
  所以「golden 里不许出现 `domain_strategy`」那条断言不是锦上添花,它是唯一的防线。

  隔壁那个坑形状正好相反:老式的 `{"address":"local"}` 会被**明确拒绝**
  (`legacy DNS server formats … removed in sing-box 1.14.0`),
  所以 `dns.servers` 用的是 1.12+ 的 `{"type":"local"}`。两个坑都踩过一遍才写对。

  正确的写法要两半:`route.default_domain_resolver` 必须**指向一个存在的
  DNS server tag**,指不到就起不来。所以写策略时顺带保证 `dns.servers` 里有一个
  可引用的项 —— 已有的一律复用(那可能是别处配好的上游,这里只借 tag),
  没有才插一个。`自动` = 什么都不写,连空的 `route` 壳子都不留。

### 内部

- **跨语言 golden 扩到整份配置。** 原来那组只覆盖 inbound,而出站策略写的是
  顶层 `route` / `dns` —— 不扩的话,字段名、新式 DNS 写法、server tag 指得到
  指不到,全要等线上 `config.apply` 失败才知道。

  现在五个策略各落一份完整配置到 `master/testdata/outbound/`,由 agent 侧的
  Go 测试喂给**真正的 sing-box** 装配校验,五份全过。这是这个改动里唯一能证明
  「字段名对、机制对」的东西 —— 其余都只是我读文档读来的。

- 改出站策略会**推进 `config_revision`**(与名称/网卡配额那几项不同,它进
  sing-box 配置)。不推的话 agent 握手时看到版本相同就不拉新配置,
  表现是「界面上改了,机器上没变」且没有任何报错(§4.1)。
  改值与推 revision 在同一个事务里,专门测了一条。

## v0.4.0

小版本号往前跳一格,两个理由:带了一张新迁移(`007_agent_commands.sql`),
以及 **agent 自升级从「从未实测」变成实际可用** —— 那是这个项目挂了最久的
一个已知缺口(见 v0.1.0 的「已知不完整」)。

### 新增

- **服务管理页 `[u]`:升级 agent**,可以只升选中的这台,也可以一键升在线的全部。

  实现上撞到一条架构约束,值得写下来:**TUI 发不了 RPC**。
  `sbx tui` 与 `sbx daemon` 是两个独立进程,WS 连接活在 daemon 里,
  TUI 手上没有 registry 也没有 rpc。第一版直接在 TUI 里调 `rpc.call_default`,
  编译器当场拦下。

  配置类变更靠 revision 递增传达(agent 下次握手时比对),但升级**不是状态、
  是一次性动作**:它不该在 agent 每次重连时被重放一遍。所以加一张显式的队列表
  `agent_commands`,TUI 入队,daemon 的巡检循环取走下发。

  取走与标记在**同一个事务**里 —— 分开做的话两轮巡检挨得近就会把同一条指令
  下发两遍,而升级下发两遍意味着那台机器连着重启两次。
  失败原因写回那一行而不是做完就删:它是唯一能解释「为什么那台还是旧版本」的东西。

  **升到主控自己的版本**,不去查 GitHub 的 latest。§11.1 规定两边共用一个版本号,
  所以「升到主控这个版本」就是「让整个集群同步」。反过来查 latest 有一个很难查的
  后果:主控还是 0.4.0 而 agent 被升到 0.5.0,协议一旦有变化就是主控自己把集群
  升挂了,而且没人会想到来查这里。

  入队**之前**先做两件事,任一步失败就不入队:认架构(release 只出 amd64 /
  arm64,别的架构直接报错,而不是下发一个必然 404 的地址)、取 sha256
  (它是 agent 侧唯一能挡住「下到坏文件就把自己替换掉」的东西,取不到宁可不升)。
  sha256 按**架构**取而不是按台取 —— 十台 arm64 没必要把同一个文件拉十遍。

- **底栏 `[U]`:升级主控自己。** 挂起 TUI 去跑一键安装脚本,跑完接回来。

  必须挂起:脚本会打十几行进度(下载、校验、替换、restart),而
  alternate screen + raw mode 下那些输出要么看不见、要么排版全乱 ——
  出问题时那几行恰恰是唯一的线索。

  接终端的顺序是刻意的:**先把界面接回来,再判断脚本结果**。反过来的话
  脚本失败时会从那里 return,把人扔在一个 raw mode 没关的 shell 里。

  跑完**不自动重启自己**:当前进程用的还是老二进制,要重进 TUI 才会加载新的。
  回执里明说这一点 —— 悄悄替换掉再假装没事,比明说更糟。

  大写 `U` 与服务管理页的小写 `u` 差一个 Shift,但**分属不同作用域**:
  `U` 是全局的,`u` 只在那一页有。两者影响面差一个数量级。

- **仪表盘的两张表可以下钻了**(`←/→` 换栏、`↑↓` 选行、`Enter` 看明细)。
  明细本身复用节点页 / 用户页那套,不是重写。

  一个不显眼但会出错的地方:两张表都按用量排过序,而 `users` / `nodes` 本身
  是按 id 排的。选中的下标是**排序后**的行号,解析时必须走同一份次序 ——
  两边各排一次的话,光标停在第 1 行、打开的却是另一个人,而且不报任何错,
  只会给出一张「看起来正常但属于别人」的明细表。排序因此抽成共用函数,
  渲染与解析同源,并专门测了一条。

  `←/→` 在仪表盘上换栏而不是翻页 —— 两张表就是左右摆着的,这是它们在这一页
  最自然的含义。换页仍然走 `Tab` 和 `1-5`。

## v0.3.8

### 修

- **网速图中间是一条长直线,只在上报到达时跳一下。** v0.3.7 改成了「每次刷新
  记一个点」,而主控在两次上报之间**根本没有新信息** —— 同一个数字被复制
  一大串,画出来自然就是一条直线。画得再密也变不出不存在的数据。

  改回按上报记:每个点都是一次真实测量,中间不再有重复段。曲线走多快由
  `cluster.report_interval_secs` 决定 —— 调到 1 秒就和 sb-manager 逐格对应。

  顺带把两者的差别记在这里,免得以后再绕一圈:**sb-manager 没有 agent**,
  它跑在被监控机器本机上,每秒直接读 `/proc/net/dev`,所以天然有 1 秒分辨率。
  sbx 的主控与被控不在同一台机器上,唯一的数据来源是 agent 报上来的数字,
  分辨率被上报间隔硬性锁死。这不是渲染能解决的问题。

- **一次左移看不出来。** 窗口从 120 格收窄到 **60 格**(与 sb-manager 一致)。
  点数少一半,每格就宽一倍,于是每来一个新读数、曲线往左挪的那一下看得见;
  120 格时一次位移只有半个字符宽。

  覆盖多长时间跟着上报间隔走:1 秒 = 一分钟,5 秒 = 五分钟,30 秒(默认)= 半小时。

### 说明

- **上报间隔的默认值没动**(仍是 30 秒)。想要 sb-manager 那种流畅度,
  去设置页(按 `5`)把「上报间隔」调到 1~5 秒即可,代价是每台 agent 的
  DB 写入与 WS 消息按倍数上涨 —— 那该由部署规模决定,不该替所有人改掉。

## v0.3.7

### 修

- **网速图上只剩孤零零几个点。** v0.3.6 把横轴改成按容量算,斜线是没了,
  但图上大半时间是空的 —— 因为根子不在横轴,在**采样太稀**:
  点是按 agent 上报记的,30 秒一个,要一小时才铺满 120 格。
  (sb-manager 每秒一个点、60 点,一分钟就满,所以它的图一直是满的。)

  改成**每次刷新记一个点**(`TICK` = 1s,120 点 ≈ 两分钟)。原来那条
  「按上报记」的理由是「一轮上报会被复制成 30 个一样的点,横轴就从时间
  变成了刷新次数」—— 那是错的:每秒一个点,横轴**就是**时间,一秒一格。

  横轴也跟着改回按当前点数走:曲线从第一帧起就铺满整幅图宽,攒满之后
  变成一个往左滚的窗口,与 sb-manager 一致。留白本来是拿来遮「采样太稀」
  的丑的,那个根子已经由上面那条治了。

  一句实话:底层读数每 30 秒才变一次,所以曲线是**阶梯状**的 ——
  一段平台接一个跳变。那不是渲染的毛病,是这个数字本来的分辨率
  (30 秒平均值,§8.2),一个周期内保持不变才是对的。

### 改

- **删掉「详情」面板 —— 它和「操作」面板重复了。** 节点页的详情写着
  `#1 vless · vless-reality · 监听 443 · 在 azure 上`,而底下操作栏第一行
  写着 `选中: vless  机器: azure  协议: vless-reality  端口: 443`:
  同一屏上把同一件事说了两遍,还各占四行。用户页同理。

  sb-manager 只有一个「操作」面板,没有单独的详情。照做:两个详情面板整块
  去掉,把它们**独有**的那几项(完整 SNI / path、中转落点、订阅导出族、
  端口复用、订阅地址、网卡绑定说明)搬进操作栏的摘要行。摘要行因此可以是
  两行,面板高度跟着内容走。省下的四行还给了表格。

- **仪表盘不再有「操作」面板。** 它是只读页,摆一个只写着「要动手请去别处」
  的空框纯属浪费四行,而那四行还给了折线图和两张列表。sb-manager 的仪表盘
  也没有这个面板。

### 内部

- 删面板的同时把它守着的保证一并搬走,不是删掉:中转落点(「客户端连这里,
  不是节点自身端口」)、订阅地址、「未分配节点」,以及 **§11.3:密钥材料
  绝不上屏**。

  最后一条尤其要紧:摘要行现在要从 `NodeParams` 里取 `server_name` /
  `path` / `relay`,而那个结构里还躺着 reality 私钥、证书私钥、
  ss 服务端密钥 —— 取的时候一不小心就会把整个 params 塞进去,
  而那是个**没有任何报错**的泄露。表格那一侧的守卫留在原处,
  摘要行这一侧新加了一条。

## v0.3.6

### 修

- **网速图里多出一条竖线,看起来像两组数据。** 那是 y 轴的轴线 ——
  只要给了 y labels,ratatui 就会腾出一列来画它。峰值改写进标题
  (`↑ 上行  1.2 KB/s   峰值 8.4 KB/s`),轴线和那一列一起去掉。

- **刚打开界面时曲线被拉成一条横贯全屏的斜线。** 横轴原来按「当前有几个点」
  算,3 个点就铺满整幅图宽,相邻两点之间插出几十列长的直线。
  现在横轴固定为 `HISTORY_LEN`(120 点 ≈ 一小时的容量),数据从左往右长 ——
  四个点就是左下角四个点,右边留空。

  留空才是诚实的:那条斜线把「还没有数据」画成了「稳步爬升」,
  是在编一个不存在的趋势。v0.3.5 修掉的是**假的第一个点**,
  这次修的是**横轴口径**;两者叠在一起才让那条斜线彻底消失。

- **`clash_reality_carries_public_key_not_private` 偶发失败。**
  断言是拿原值拼出来的:`yaml.contains(format!("public-key: {pbk}"))`。
  而 reality 公钥是 **base64url**(字母表含 `-` 与 `_`),`-` 开头的 YAML 标量
  会被 `yaml_str` 加引号 —— 于是 64 分之一的概率上真实输出是
  `public-key: "-abc"`,断言却在找 `public-key: -abc`。

  **产品行为是对的,错的是这条断言。** 每跑一次都现生成一对新密钥,
  所以它大多数时候绿、偶尔红,而重跑总会绿 —— 最容易被一直重跑过去的那种形状。
  现在比对走 `yaml_str`,并另加一条把那个罕见分支钉死(公钥手动设成 `-` 开头):
  不钉的话它平均 64 次才走到一次,等于没被测过。

  这也是 v0.3.4 打 tag 时那次 `1 failed` 的真正原因。当时我判成了
  `cluster::server` 里的三处 `sleep` 并去改了它们 —— 那个写法确实脆、改了不亏,
  但它不是病因。判错的直接原因是:只重跑了单个测试,而且没记下测试名。

### 改

- **底部两条栏的次序对调,并把当页操作装进一个带边框的面板**(照 sb-manager)。
  现在是:「操作」面板在上(第一行**选中了什么**,第二行**这一页能按的键**),
  通用信息行(版本 / 规模 / 切页退出)在**最底下**。v0.3.5 把这两条放反了 ——
  版本那一行夹在主体和操作栏中间,会让人以为它属于当页的操作。

  选中项摘要和按键放进同一个框是有意的:「[d]删除」和「选中: alice」隔开摆的话,
  按下去之前得先抬头去表里找光标在哪 —— 而那正是最不该需要确认两次的时刻。
  用户页的摘要里直接带**完整订阅地址**(撤销过的显示「(已撤销)」而不是一条
  必然 404 的 URL)。

## v0.3.5

### 修

- **网速折线图画出一条从左下到右上的完美斜线。** 那不是流量曲线,是**两个点
  之间的插值**:`load_agents` 每帧都往历史里记一个点,而刚打开界面时没有任何
  agent 有第二个采样点可比(速率全是 `None`,求和得 0.0)—— 于是第一帧记了个
  `(0, 0)`,下一轮上报记真实速率,历史里只有两个点,ratatui 把它们拉满整个图宽。

  它看起来像「流量在过去一小时里稳步爬升」,因此没人会去查。**上下行两张图
  长得一模一样**本该是线索:真实流量的上下行不可能逐点等比。

  判定挪进了 `SpeedTracker::observe` 自己(多收一个「有几个读数」的参数),
  不是留在调用方 —— 调用方少判一次就又变回那条假斜线,而它不报任何错。
  `known == 0` 不是「全网速率为 0」,是「还没有任何可比的采样」,
  这与速率字段用 `Option` 而不是 `0.0` 是同一条理由。

### 改

- **底部工具栏拆成两条**(照 sb-manager 的做法)。倒数第二行是通用信息栏
  `sbx v0.3.5  用户:N  节点:M  机器:K  [1-5/Tab]切页  [↑↓/jk]选择  [R]刷新  [q]退出`,
  最后一行只回答「在这一页我能做什么」。

  原来两者挤在一行,于是「切页/选择/刷新/退出」重复五遍,把真正会变的那部分
  ——当页能做什么——挤到行尾,窄终端上正好被截掉。
  回归测试断言的是**渲染出来的字符**,并且明确断言底栏**不再**出现通用键 ——
  重复正是这次要修的那个毛病。

- **仪表盘节点视图去掉进度条。** 进度条的含义是「用了多少 / 上限多少」,
  而节点没有上限 —— 配额只存在于用户和整机网卡两个层面(§6.3)。
  那条画的其实是「占全网份额」,可它和别处的配额条长得一模一样,
  于是一个承载了 95% 流量的**健康**节点看起来像「快爆了」。
  现在只留百分比、改中性色(跟着比例变红等于把「承载得多」染成告警),
  标题写明口径:`节点用量(% = 占全网份额)`。

### 新增

- **用户页 `[T]` token 管理**:`[g]` 重新生成(老 URL 立即失效)、
  `[v]` 撤销(订阅返回 404,`[g]` 可恢复)。

  撤销与「停用用户」是两件事:停用挡的是**代理连接**(§7.5),撤销挡的是
  **订阅下载** —— 一个泄露了订阅链接但还该上网的用户,要的是前者。

  实现上有一处不能照抄旧项目:它撤销时把 `sub_token` 写成空串,而 sbx 这一列是
  `NOT NULL UNIQUE`,空串在**第二个**被撤销的用户上就撞唯一索引
  (表现是「第一个能撤,后面的报数据库错误」)。这里写的是 `!revoked:<id>`:
  带 `!` 和 `:`,过不了订阅接口的字符集校验,在查库之前就被挡下返回 404;
  带上 id 又保证每行互不相同。

  两条测试盯着这件事:一条验「写进去的值过不了格式校验」,一条走**完整 HTTP
  路径**验撤销前能下、撤销后 404、且老地址一起失效 —— 中间任何一环把它当
  普通 token 去查库,撤销就静默失效了。

  顺带:`[s]` 订阅框遇到已撤销的 token 不再照常拼一条必然 404 的 URL 给出去,
  而是先说清「是你自己撤的,按 `[T]` → `[g]` 恢复」。

- **用户页 `[r]` 重置流量**。只清零本周期用量,**不动**月重置日期。

  与周期重置的区别是不推进 `last_reset_ym` —— 推了的话这个月的自动重置就被
  跳过,而管理员按这个键的意思是「现在清一次」,不是「这个月不用再清了」。
  `total_*`(终身累计)和 `last_up / last_down`(delta 基线,§5.2)同样不动:
  动基线会让下一次上报被当成新纪元,把一整个周期的量重复计一遍。

### 内部

- **三处测试里的 `sleep` 换成等条件。** `cluster::server` 的三个测试原来是
  「做一件异步的事 → 睡 200~300ms → 断言库里已经写好了」,机器一忙就可能
  还没写完。v0.3.4 打 tag 那次的 `1 failed` 大概率就是这么来的 ——
  之后 47 次重跑全绿,没能复现,所以这里不声称修好了那一次,只是把这个写法换掉。

  真正的代价不在测试,在它训练出来的习惯:那次我看到 `1 failed` 之后照样让
  commit 和 tag 跑下去了。「红了就重跑、重跑绿了就当没事」一旦成立,
  下次真的回归也会被同样放过去。

  两个辅助函数,方向相反:`wait_for` 等某件事发生(轮询到成立就返回,
  5 秒超时并报出在等什么);`stays_true` 在 300ms 窗口内反复断言条件**一直**成立,
  给「某件事不要发生」用。后者不能用轮询写 —— `is_online()` 一开始就是 true,
  轮询会立刻返回,而那条可能出问题的清理路径还没来得及跑,
  测试永远绿却什么都没验证。两个辅助函数都**验过会失败**(把条件改成恒假,
  确认 panic 且信息可读):一个不会失败的测试辅助比没有更糟。

## v0.3.4

### 修

- **绑了网卡之后,订阅页上的数字没变。** 换掉的只有响应头
  `subscription-userinfo`,网页正文还在显示账号自己的用量 —— 绑完打开页面
  看到的还是原来那个数,像是功能没生效。

  而这正是这个功能存在的理由:VPS 厂商按网卡计费,管理员要能用**任意一个
  代理客户端或者浏览器**看到「这几台机器这个月烧了多少」,不用 ssh 上去查。
  现在页面上的「已用 / 上限 / 上行 / 下行」跟着响应头走:绑了网卡就是网卡口径,
  没绑就照旧是账号自己的计费用量。

  **账号状态(正常 / 超额 / 停用)仍然按账号自己的用量判** —— 绑网卡是一个
  纯显示功能,不该能把人停掉(§10.3)。所以页面上多一行
  「账号自身:XX(停用判定按这个数)」:少了它,「已用 900 GB」旁边挂着一个
  「正常」会像是个 bug,而那恰恰是账号的真实状态。网卡烧爆了也不会把状态染红,
  那是厂商的账,不是这个号的账。

### 改

- **新用户的计费倍率默认从 1.0 改成 2.0(双向)。**

  口径的事实:agent 的 tracker 记的是「客户端 ↔ 本机」那一段的 read + write,
  而代理要把同样的数据**再跟目标站点跑一遍**(客户端→本机→目标,
  目标→本机→客户端)—— 网卡上进出的量约是 tracker 数字的两倍,
  而 VPS 厂商正是按网卡计费。默认 1.0 等于「用户账单只到厂商账单的一半」,
  是在替管理员亏钱,而且是一个不会有人发现的亏法。

  **只影响新建的用户,已有用户一个字节不动。** 老用户的倍率是管理员当时
  定的价,批量翻倍等于在所有人不知情的情况下把配额砍一半 ——
  会让一批人在下一次巡检(30 秒)里直接被停用。要改老用户就去用户页按 `[E]`,
  或者自己写 UPDATE。

  默认值写在代码里(`node_repo::DEFAULT_TRAFFIC_MULTIPLIER`)而不是 schema 的
  `DEFAULT`:SQLite 改不了列默认值,只能重建整张表,而 `users` 被 002 / 003
  各加过一批列,重建就得把当前列一个不漏地抄一遍 —— 抄漏一列的后果是
  那一列的数据在**人已经在用的库**上直接消失,而且这段抄写会随着以后每加一列
  而腐烂。006 那张迁移是空的(只有注释),就是给这个决定留个落点。

- 用户表单里倍率那一项的措辞改成「计费倍率 (2.0 双向 / 1.0 单向)」,
  并在说明里写清「双向」指的是代理链的两段,不是一段的上下行。
  新增用户的表单也标出默认值是 2.0。

## v0.3.3

### 改

- **仪表盘底部换成「用户视图 | 节点视图」。** 原来右边是被控机的网卡与配额,
  和左边的用户用量并排 —— 而这两组数字口径不同(§6.4):网卡是
  `/proc/net/dev` 的整机进出,含系统更新、别的服务、协议开销;用户量是
  sing-box 记的账。厂商按前者计费。并排摆着的唯一效果,是让人反复怀疑
  「这两个数怎么对不上」——那不是 bug,是两个问题的两个答案。
  现在两栏同一口径,可以直接比。

  节点栏按本周期用量排,条形画的是**占全部节点的份额**而不是配额比例
  (节点没有配额,「量堆在哪」才是这一栏回答的问题)。标签是 `tag@机器名`:
  tag 在不同机器上可以重名,只有 tag 的两行会长得一模一样。

- **网卡明细搬到服务管理页的二级页面(按 `Enter`)**,和节点页 / 用户页
  同一个手势。表头五行是整机口径(本周期 ↑↓、配额条 + **剩余绝对值**、
  重置日与还有几天、当前速率、CPU / 内存 / 负载),下面的表是这台机器上
  各节点跑了多少。两个口径写在同一张纸上,比藏起来一个要好。

- **用户页 `[s]` 的订阅框按当前配置说三种不同的话。** 默认订阅服务只听
  `127.0.0.1:18081`,原来那版不管三七二十一给一条打不开的 URL ——
  人会去查 token、查节点配置,而那个方向永远查不出结果。现在:服务关着就
  直说「这条地址一律返 404」;没配对外地址就把 nginx 反代和直接对外听两条路
  连同后者的代价一起印出来;配好了才给完整地址,并且能按 `y` 复制 ——
  二十位的随机 token 靠手抄是抄不对的。

- **设置页里「订阅对外地址」与「主控对外地址」的说明重写。** 两者不重复:
  前者是给用户的订阅 URL(带协议),后者是被控机回连的主机名(不带协议与端口)。
  只是后者留空时会**借用**前者的域名当兜底 —— 有域名就别用裸 IP。
  原来的说明把这层借用写在前者头上,读起来像两个设置管同一件事。

### 修

- **节点视图那一栏的百分比会被切掉。** 列宽原来是拍的常数,在 120 列屏幕上
  正好差一格。`0%` 被切成 `0` 看起来像一个正常数字 —— 没人会发现这一列是残的。
  现在宽度从内框往回算,让位时**整列整列地丢**(先条形、再百分比):
  挤压某一列 ratatui 是不吭声的,而少一整列人一眼就看得出来(§13.4)。
  回归测试盯 50 / 57 / 80 / 100 / 120 / 160 六档。

- **明细表右边的说明是被 `Paragraph` 无声截断的。** 切出来的 `vless-real`
  看着像一个完整的协议名。改成自己截,`vless-rea…` 一眼就知道被截了。

### 内部

- **agent 自升级补上了自动化测试**,这是最后一处没有覆盖的功能。
  它的失败代价不对称:升级失败无所谓(老版本继续跑),但把 `sbx-agent` 换成
  一个下了一半的文件,会让这台机器在 systemd 下次重启它的那一刻永久掉线,
  现场只留一个 `Exec format error`。八条用例,每条失败路径都断言
  **老二进制一个字节没变、临时文件没残留**:sha256 不符、下载被掐断、
  HTTP 404、sha256 格式非法(且必须在发请求之前挡掉,不为一条明显写错的
  指令白下 38 MB)、目录不存在;软链要换指向的实际文件,不能把软链本身
  覆盖成普通文件。

- 清掉两处过期注释。其中「组装失败要记审计且不断连」那条测试,注释说靠
  「trojan 未实现」触发,但八个协议早就都实现了 —— 它实际靠的是缺证书参数。
  哪天给 trojan 补个自签兜底,这条测试就会在无人察觉的情况下不再测它声称
  要测的东西。改成用本版本读不懂的协议名触发,并断言了失败原因。

## v0.3.2

### 修

- **刚发布的版本偶尔装不上。** 新 release 在 GitHub 的 CDN 上有几秒传播窗口,
  那期间同一个下载地址会间歇性返 404 —— 而 `curl --retry` 默认**只重试传输层
  错误和 5xx,不管 404**。表现是「刚发的版本下载失败」,过一分钟又好了。
  (`--retry-all-errors` 能覆盖,但它要 curl 7.71+,被控机上可能更老,
  传了会直接报「不认识的参数」。所以退回最朴素的办法:自己睡 3 秒再试,最多三次。)

- **发版流水线上传的资产列表里有重复,v0.3.2 第一次发版就栽在这。**
  `release.yml` 里那五条 glob,`dist/sbx-agent-*` 和 `dist/*.sha256` 会同时命中
  两个 agent 的 `.sha256`,同一个文件在上传列表里出现两次。
  `action-gh-release` 是 `Promise.all` 并发传的:两次上传各自先查
  「同名资产在不在」——都答不在,然后一起 POST,后到的那个吃 422
  `already_exists`,整步就红。谁先到看网络,所以它**偶发** —— 前九次发版
  都没事,第十次炸了。

  更难查的是它**炸完长什么样**:非 prerelease 的 release 是先建成 draft、
  资产传完再翻成正式的,所以中途炸掉留下的是一个**草稿** release ——
  未登录去看 `/releases/tags/v0.3.2` 返回 404,看上去像「压根没建过」,
  而实际上它就躺在仓库里。

  现在让每个文件只被一条 glob 命中,另外打开 `fail_on_unmatched_files`:
  glob 收窄之后,谁改了打包步骤的文件名这里就该红,
  而不是默默发一个少几个资产的 release。

## v0.3.1

### 修

- **页签整条不见了。** v0.3.0 换成 ratatui 的 `Tabs` + `Borders::BOTTOM` 之后,
  布局里还是只给了一行 —— 那一行被下边框吃掉,屏幕上只剩一条横线。
  界面「看起来只是少了点东西」,而实际是导航条整个没了。
  新的回归测试断言的是**渲染出来的字符**,不是布局参数:参数对不对只有画出来才知道。

- **主控地址探到的是厂商给的内网地址。** `UdpSocket::connect` 读的是本机网卡地址,
  而云主机(Oracle / 阿里云 / AWS)的公网 IP 在厂商的 NAT 上 ——
  拿内网地址拼出来的接入命令,被控机根本连不上,报错还是「连接超时」:
  一个指向网络问题、其实是地址错了的错误。
  现在先**问外面「你看到我是谁」**(与 agent 侧用同一组 endpoint),拿不到才退回本机视角。
  探测在 TUI 启动时丢到后台跑,不在按下 `a` 的那一刻同步去打 —— 后者会冻住界面几秒。
  另加 `cluster.public_host` 配置项(设置页里能改),给「自动也定不对」留最后一手。

### 新增

- **订阅按网卡流量报**(用户页 `[b]`,§10.3)。给用户绑一台或多台机器的网卡流量之后,
  **只有订阅响应头 `subscription-userinfo` 里的三个数字**换成这些机器的网卡用量之和。

  VPS 厂商按网卡计费,而用户流量是 sing-box 记的账,两者天生对不上(§6.4 / §7.2)。
  绑一下之后用任何一个代理客户端打开订阅就能看到「这几台机器这个月烧了多少」,
  不用 ssh 上去查 —— 客户端本来就会把这个响应头显示成一个进度条。

  边界守死:订阅内容、用户自己的计费与停用判定、界面上的用户用量全走原来的路。
  一个只影响「显示」的功能不该能把人停掉,这一点有专门的回归测试。
  两条具体规则:**不乘倍率**(网卡是厂商的账,倍率是给用户计费的);
  **只要有一台没设配额,总量就报 0(不限)**——把设了配额的几台加起来当上限,
  会给出一个看起来精确但根本不是上限的数字,而客户端会拿它算百分比。

  界面上标得出来:用户列表多一列「订阅口径」(绑了显示 `网卡×N`),
  详情面板再写一句说明。不标的话,那是一条永远查不明白的「客户端和后台对不上」。

## v0.3.0

### 修(两个都会让界面上的数字骗人)

- **网速几乎总是显示 `--`,偶尔闪一下。** TUI 每秒刷新,agent 每 30 秒上报一次 ——
  中间 29 次刷新做不出差,而那一版在做不出差时直接给 `None`。于是网速只在上报
  到达的**那一帧**出现。这不是「没有数据」,是「数据还是上一次那个」:它本来就是
  30 秒平均值,一个周期内保持不变才对。现在缓存上一个读数,超过 90 秒
  (上报周期的三倍)没有新的才退回 `--` —— 那时是真掉线了。

- **新接入的机器凭空多出几十上百 GB 网卡流量。** 网卡计数读的是 `/proc/net/dev`,
  它从**机器开机**就在数。第一次上报时照通用 epoch 规则「delta = new」办,
  就把这台机器开机以来的全部流量搬进了本周期用量,而它在本集群里一个字节都还没跑。
  现在第一次上报只建基线、不入账。机器**重启**那条路不变 —— 那时计数器确实从 0
  开始,`delta = new` 是对的;区别在「有没有基线」,不在「epoch 变没变」。

### 新增

- **设置页(第 5 页)。** 订阅对外地址/监听/开关、集群监听/TLS/心跳/上报间隔、
  Telegram 开关与 token / 管理员 / 时区,都能在界面里改,不用去 ssh 里编辑
  `config.toml`。布尔项按一下就切,其余开一个单字段表单。
  写回是**行级替换**,配置文件里的注释与排版全部保留 ——
  反序列化再写回会把那几十行解释一次抹掉,而那是唯一说明「这个端口为什么只听本地」的地方。
  `bot_token` 在列表里只显示前 6 位,编辑框**不预填**旧值(§11.3)。
  页面顶上常驻「改完要重启 daemon」——daemon 启动时才读配置。

- **用量明细二级页面**(节点页 / 用户页按 `Enter`):
  节点 → 这个节点上各用户用了多少;用户 → 这个用户在各节点上用了多少。
  跨 agent 的用户在列表里只有一个合计,分不出是哪台机器在承载。
  分配了但没跑过流量的关系也会显示(全 0)——「分配了没生效」正是最需要看见的。

- **仪表盘的上下行盲文点阵折线图**,照旧项目 `pages/dashboard.rs` 的做法。
  一个点 = 一轮上报(30s),不是一次刷新 —— 后者会把一轮上报复制成 30 个一样的点,
  横轴就从「时间」变成了「刷新次数」。120 个点 ≈ 一小时;矮终端上整块让掉。

- **`R` 立刻刷新**。大写是刻意的:小写 `r` 在服务管理页是「轮换 token」,
  那是一个不可撤销的动作,不能和刷新只差一个 Shift 却又长得像。

- 新增 agent 的表单里可以直接填**网卡月配额与重置日**,不用建完再进一次编辑框。

### 改进

- **顶栏改成旧项目的样式**:`仪表盘[1] │ 服务管理[2] │ …`,底下一条分隔线。
  首页也改叫「仪表盘」。

- **主控地址不再手填。** 配了订阅域名就用域名(换机器时被控端不用重配,
  TLS 那一套也才说得通);没配就探本机出口地址 —— `UdpSocket::connect` 只让内核
  选一条路由,**不发包**、不依赖外部服务、不会卡住。NAT 后面探到的是内网地址,
  那时去设置页填订阅对外地址覆盖。不用 ipify 之类的外部服务:那会在无外网的
  内网部署里卡住十几秒,还给 TUI 引入一个网络依赖。

- **表单里选中那一格换了配色。** 之前是黄底深字,而很多终端配色会把 `Color::Black`
  渲染成一个偏亮的灰 —— 结果是黄底浅字,对比度反而比不选中还低。
  现在是深灰蓝底 + 原色前景,不依赖终端把黑渲染成真的黑。

- 上下行流量到处都用 ↑/↓ 标出来(概况、用量 Top、用户详情、明细表),
  不再只给一个合计。

- 仪表盘去掉「最近事件」面板。

### 移除

- `tui::data::load_events` 与 `EventRow`(随最近事件面板一起去掉)。
  `agent_events` 表本身还在写,查它现在要用 `sqlite3`。

## v0.2.2

二级页面的排版照旧项目 `sb-manager` 重做了一遍,并给接入命令加了一键复制。

### 改进

- **表单从「一个字段三行」压到两行。** 标签与取值同一行(标签列按最长的可见标签对齐,
  所有取值落在同一竖线上),下面空一行;聚焦的取值用反白底。
  提示并进标签的括号里(`端口 *必填`、`协议 (←/→ 切换)`),装不下的落到弹窗底部的说明区。
  之前每个字段占「标签 / 灰色提示 / 空行」三行,九个字段就把弹窗撑到快满屏 ——
  视线要跨过两行才能从一个输入框走到下一个,读起来是散的。

- **弹窗的宽高都按内容算,不再用百分比。** 百分比在宽屏上拉出一个空荡荡的大框,
  在窄屏上又把说明和底部按键提示切掉(之前「新增用户」的提示行末尾就被切成 `[Esc]取`)。
  宽度取「字段行 / 最长说明 / 按键提示」三者的最大值。

- **说明区自己折行**(`theme::wrap`),不再交给 ratatui 的 `Wrap`。
  后者折出来的行数算不进弹窗高度,底下几条会被静默裁掉;而且续行顶到最左边,
  看起来像另起了一条说明。

### 新增

- **接入命令按 `y` 一键复制**,走 OSC 52。
  不用原生剪贴板库是因为主控通常是一台无头 Linux,人从自己电脑 ssh 进去 ——
  原生库操作的是服务器那侧的 X11 剪贴板,在那儿要么报错、要么复制到一个没人看得见的地方。
  OSC 52 把内容发给终端模拟器,由它写进用户本地剪贴板,天然穿过 ssh。
  代价是没有回执:终端不支持(或 tmux 没开 `set-clipboard`)就什么都不会发生,
  所以弹窗里那句回执如实这么写,没有说成「已复制成功」。

- `sbx agent-add` / `agent-rotate` 在**普通终端**里也打出同一条命令。
  这是不依赖任何终端特性的退路 —— 那里用鼠标选中复制没有任何前提。
  (顺带:`agent-add` 以前只打一份要人手抄的 `agent.toml` 片段。)

## v0.2.1

### 新增

- **概览页显示每台机器的 CPU / 内存 / 负载 / 运行时长。**
  这几个值 agent 一直在报(`sysinfo.report` 每 30 秒一次),但主控入库时只取了
  网卡那一段,其余原地扔掉 —— 概览页想显示「哪台机器忙成什么样」时无米下锅,
  而数据其实早就在线上了。现在落在 `agents` 表上(`004` 迁移),存最新一次即可:
  它们和网卡计数不是一类东西,网卡是累加的账、必须一笔不漏,CPU 只有
  「现在多少」有意义。
  同一张迁移里加了 `sysinfo_at`,而且它不是可选的 —— 少了它,一台离线三天的机器会
  一直挂着三天前那个「CPU 3%」,看起来和一台闲着的在线机器一模一样。
  超过 90 秒(上报周期的三倍余量)整行显示 `--`。
- 服务管理页在 140 列以上多一个「主机」列(CPU / 内存),窄了就不画 ——
  它是最锦上添花的一列,不该从流量或重置日那里抢地方。

## v0.2.0

TUI 这一轮基本重做了一遍二级页面,并让「新增被控服务器」直接给出可以整条复制的接入命令。

### 新增

- **概览页**(页签 1)。集群概况(在线/离线/从未连接、节点数、用户启停分布、
  总网速、本周期用量)、用量 Top、各 agent 的配额条、**最近事件**。
  最后一项是刻意加的:计数器重置、配额自动停用、配置下发失败都记在 `agent_events` 里,
  在此之前那张表只能靠 `sqlite3` 手查 —— 而「界面上数字不对」第一个该看的就是它。
  「本周期用量」同时给网卡与计费两个数字并写清口径,它们天生对不上(§6.4 / §7.2)。

- **页签带序号,数字键直达。** `1` 概览 / `2` 服务管理 / `3` 节点 / `4` 用户。
  只有 Tab 循环的话,去最后一页要按三下,而人心里想的是「去第 4 页」。

- **新增 agent 直接给出一键接入命令**,而不是一份要人手抄的 `agent.toml`:

      curl -fsSL .../install.sh | SBX_SERVER='wss://…/ws' SBX_TOKEN='…' SBX_FINGERPRINT='sha256:…' bash

  `install.sh` 认这三个环境变量,收到就写好 `/etc/sbx/agent.toml`(**0600**)并
  `enable --now sbx-agent`。给了 `SBX_TOKEN` 就一定是在装被控端,所以连
  `SBX_TARGET=agent` 都不用带 —— 那条命令已经够长了。
  轮换 token(`[r]`)同样给出带新 token 的完整命令,在那台机器上重跑一遍即可,
  旧配置自动备份成 `.bak`。
  主控地址只有人知道(公网 IP / 内网 IP / 域名),表单里问一次,
  默认值从 `subscription.public_base` 的主机名猜一个;没填就在命令里留占位符**并说出来**,
  而不是给一条看起来能用的错命令。

- **节点与用户都能编辑了**(`[E]`),agent 也能改名称/网卡配额/重置日。
  以前这些只能改库。

- **节点分配改成多选框**(用户页 `[n]`):打开时已分配的节点是勾着的,
  空格勾选、`a` 全选、Enter 整体替换。以前要人先去另一页把 `node_id` 记下来,
  一次只能加一个,取消分配还得走另一条路。

- `packaging/test-install.sh`:`install.sh` 的离线测试(34 条),CI 里用 dash 和 bash 各跑一遍。
  这个脚本过去唯一的验证方式是**在真机上跑一遍**,v0.1.2 的回归正是这样溜出去的。

### 改进

- **表单的字段有类型了。** 协议、所属 agent 这类取值来自有限集合的字段改成 `←/→` 循环
  的选择器 —— 以前协议是**手打**的,打错一个字母的反馈是提交之后一句
  「无法识别的协议」,而正确的八个值一个都没显示出来。
  开关(订阅优先 IPv6)用空格或 `←/→`。

- **字段按协议显隐。** `server_name` 只对 reality/trojan/tuic/anytls 出现,
  `path` 只对两个 ws 协议出现 —— 判定与 `secrets::fill`、`service::build_inbound`
  是同一份事实。让 shadowsocks 的表单上摆着一个填了不生效的 `path` 框,
  比不显示它更容易出错。协议一换焦点会自己挪走,不会卡在一个已经消失的框上。

- 节点页与用户页加了底部**详情面板**(完整 SNI、中转落点、订阅地址),
  以及更多列(上/下行拆分、重置日、倍率、中转、导出族)。
  快到期的用户在到期列里直接显示「还剩几天」。

- **窄终端下宁可砍整列,不要压窄每一列。** 砍掉一整列是看得见的,压窄一列是看不见的
  —— 80 列下「每月 22 日重置」被切成「每 」而界面看起来毫无异常,就是后者的代价。

- 宽度一律按**显示列数**算,不再按字符数:一个汉字占两列,
  `format!("{:<12}", "计数器重置")` 会在一个已经占 10 列的串后面再补 7 个空格。

### 修

- `install.sh` 里 `curl … && systemctl …` 那条 && 链:unit 下载失败时
  `set -e` 会让整个脚本**一声不响地退出**,人看到的是「装了一半」。
  现在下载附属文件失败会明说,并且继续把能做的做完。
- `write_agent_config` 的 `umask 077` 会漏到后面的步骤,顺手改掉别的文件权限;
  现在存下来再改回去。
- 两个 systemd unit 里的 `Documentation=` 还写着 `yourorg/sbx`。

### 已知不完整

- TUI 仍然只在无头渲染(`TestBackend`)和按键单测里覆盖过,
  没有人在真实终端里从头点到尾。**新加的四个页面与全部表单都有渲染快照测试**,
  但那和真终端里的观感是两回事。

## v0.1.3

### 修

- **裸命令直接装主控。** 现在

      curl -fsSL .../install.sh | bash

  就装主控,不用再记 `-s --`。选目标的优先级:命令行参数 / `SBX_TARGET`
  > 本机已装的部分 > 默认主控。中间那条不能省 —— 一台只跑 agent 的被控机上
  跑裸命令,意图显然是升级那个 agent,而不是给它安一个主控。

- **v0.1.2 的回归:带参数调用一律报「SBX_TARGET 只能是 master / agent / all」。**
  `parse_args` 拼参数时用的是 `TARGETS="$TARGETS $1"`,会拼出前导空格(` master`),
  而 v0.1.2 新加的那条 `case` 精确匹配 `master`,对不上就 die —— 于是
  `sh -s -- master` 直接失败。

  两处都改了:拼接改成不产生空格并自动去重;那条校验本来只该管 `SBX_TARGET`,
  现在挪到读环境变量的地方,不再对命令行参数生效(命令行的合法性由 `parse_args`
  自己的 `case` 保证)。

  这个 bug 能溜出去,是因为我验证 v0.1.2 时管道那条命令拉的是 GitHub 上的脚本 ——
  而那时候改动还没推,拉到的是旧版。**改了脚本就要测那个文件本身,不是测线上的同名文件。**

## v0.1.2

### 修

- **`install.sh` 的错误提示会误导管道用户。** 没装过又不指定目标时,它一律提示
  `install.sh agent` —— 但 `curl … | sh` 的场景下照抄过去写成 `sh master`,
  shell 会把 `master` 当成**脚本文件名**去打开,报 `cannot open master`。
  现在提示按**实际调用方式**生成:管道就给 `curl … | sh -s -- agent`,
  当文件跑就给 `./install.sh agent`。
- 顺带支持 `SBX_TARGET=agent`,任何调用形式都能用,不必记 `-s --`。

## v0.1.1

### 新增

- **一键安装 / 升级脚本** `packaging/install.sh`(POSIX sh,被控机上可能只有 dash)。

      curl -fsSL https://raw.githubusercontent.com/why1f/sbx/main/packaging/install.sh | sh

  不带参数时**按本机已装的东西升级**:装了什么就升什么,一个都没装则报错并让你
  显式选 —— 免得在一台只跑 agent 的机器上莫名多出一个主控。
  已是最新版就什么都不做;下载后**强制校验 sha256**,取不到校验和宁可拒装;
  替换走同目录 tmp + `mv`(跨文件系统的 mv 不是原子的);
  只在单元**本来就在跑**时才重启它。

- `sbx-agent --version`。v0.1.0 没有这个,导致升级脚本认不出已装版本、每次都重装。
  输出走 stdout 且不带日志前缀,脚本可以直接取。

### 修

- 新版 clippy 的 `some_filter` lint(`Some(x).filter(|_| cond)` → `cond.then_some(x)`)。

## v0.1.0

首个版本。

### 主控(Rust,MIT)

- WS 集群服务端:token 握手、同 agent 重连驱逐旧连接、心跳与 `last_seen` 记账。
- 握手补齐(§4.1):`config_revision` 与 `user_state_revision` **分别**比对,
  各自触发 `config.apply` / `user.state`。两者是独立计数器,合并会造成
  「离线期间的禁用永久失效」或「高频路径退化成全量 box 重建」二者之一。
- 上报入库走 epoch/delta(§5.2):agent 报单调累计值,主控按 epoch 判断
  取差值还是取全量。agent 重启、box 重建、机器重启三种情况分别有对应输入的测试。
- TLS(§1.3):证书不存在时自签一张,启动打印 SHA-256 指纹供 agent TOFU 固定。
  指纹算法与 `openssl x509 -fingerprint -sha256` 交叉验证过。
- 配额与到期巡检(§6.3):月重置(含短月)、配额超限、到期、跨 agent 流量求和。
- CLI:agent / node / user 的增删改查,`agent-add` 生成 token(只显示一次),
  `user-sub` 打印订阅地址与分享链接。
- 订阅导出(§10):base64 分享链接 + Clash/Mihomo YAML,由 `/sub/:token` 提供。
  节点地址取自**该节点所属 agent** 的 `ipv4/ipv6`(旧项目是本机 IP 探测),
  保留中转覆盖与 `use_public_base_as_server` 的优先级链。
  订阅服务是「不做 Web 面板」的唯一例外,且只读:没有写接口、没有登录。
- TUI(§8,`sbx tui`):服务管理 / 节点 / 用户三页。
  agent 列表是**两行式**的,带自绘的多段渐变进度条(ratatui 的 `Gauge` 只支持单色,用不了)。
  网速由 TUI 自己按刷新节奏对 `agent_nic_traffic` 做差算出 —— 它与 daemon 是
  两个进程,读不到那边内存里的采样;`boot_id` 变化时显示 `--` 而不是一个爆炸的数字。
  **不限流量的行不画进度条**:一根满条读起来是「用完了」,与「不限」正好相反
  (这一处与参考图 `1.png` 不同,是有意的)。
- 配置组装:**八个协议全部支持**(§9.1)。密钥材料(reality 密钥对、自签证书、
  shadowsocks 服务端密钥)在**建节点时**生成一次并存进 `params_json` ——
  下发时现生成会让每次 `config.apply` 都换一套密钥,客户端静默全部失联。
  证书走**内联 PEM** 而不是路径:主控与 agent 不在同一台机器上。
  生成结果有跨语言 golden 测试:Rust 生成八份配置,agent 侧用**真 sing-box** 校验。

### agent(Go,GPLv3)

- 内嵌 sing-box(v1.14.0-beta.3),通过 `box.New()` / `Start()` / `Close()` API 控制,
  **不 fork、不打补丁**。构建必须带 `-tags with_quic,with_utls`
  (reality 依赖 uTLS,hysteria2 / tuic 依赖 QUIC);漏了会被编译期哨兵挡住,
  而不是编出一个跑不了那几个协议的二进制。
- `ConnectionTracker` 按 **(用户, inbound tag)** 二元组记账(§7.1)。
  计数器活在 tracker 里而非 box 里,所以 box 重建不清零 —— 这是 §5.2 的前提。
- 热重载按 §7.4 的顺序:`box.New` → `old.Close()` → `AppendTracker` → `Start()`,
  Start 失败回滚到旧配置。同端口重载与回滚都有跑真实实例的测试。
- `user.state` 只换禁用名单,**不重建 box**;禁用只挡新连接(§7.5)。
- WS 客户端:指纹钉扎的 wss、指数退避重连(1→60s)、对每一条 req 都回 resp
  (漏一条会让主控的串行补发卡到 RPC 超时)。
- `last-applied.json` 原子落盘,兼作冷启动配置源 —— 主控挂着时节点照常服务。

- 流量统计页(`stats_html`):浏览器打开订阅地址时返回它 —— 用量进度条、
  每个节点的分享链接 + 内联 SVG 二维码。**仍然只读**,没有任何管理能力。
  页面上每段动态文本都做了三种上下文的转义(HTML 文本 / 属性 / onclick 里的 JS 字符串)。
- Telegram 通知(§9.1):80/90/100% 三档阈值告警 + 定时流量播报,
  用户可在 bot 里自助查用量、取订阅、改通知开关。
  **告警去重按档位而不是百分比**:库里记「已通知到哪一档」,只在上升时推 ——
  按百分比去重会让 80.1% → 80.2% 也算一次变化,每 30 秒推一条。
  月重置后档位会被降回去,否则那个用户再也收不到告警。
  单实例租约(`tg_bot_lease`)保证同一个 bot_token 只有一个长轮询:
  多开时 Telegram 会 409,而且两边都随机丢 update,表现成「命令时灵时不灵」。

### 已知不完整

- **agent 自升级**(`agent.upgrade`)没有实测过 —— 它要一个真实的发布产物才能验。
- TUI 的交互只在无头渲染(`TestBackend`)和按键单测里覆盖过,
  没有人在真实终端里从头点到尾。
- Telegram bot 的收发只对着 Telegram API 的错误路径验过(假 token → 401 → 退避),
  没有对着真 bot 跑过一遍完整会话。相对旧项目也**刻意少了**管理员在聊天里
  改用户配额/到期的那套表单 —— 那些操作要推进 revision、下发配置,
  放在一个可能因为网络抖动重放的界面上并不合适,CLI 和 TUI 上都有。

### 已验证

在一台 Oracle ARM(Ubuntu 22.04 aarch64)上跑通了 §13.2 / §13.3:
1 个主控 + 2 个 agent + 真客户端流量,跨 agent 求和与断连恢复都逐字节对上。
驱动程序与复跑步骤在 `e2e/`。`-race` 也在那台机器上跑过,干净。
